# Local development — run from backend/: go run . -env=development
# Requires a local Postgres instance matching DATABASE_URL below.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/twitter_dev?sslmode=disable
SERVER_ADDR=:3000
